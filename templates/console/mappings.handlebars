<div class="card mb-4">
    <div class="card-header">
        Mappings
    </div>
    <div class="card-body">
        {{#if this.length}}
            <table class="table table-sm mb-0">
                <thead>
                    <tr>
                        <th scope="col">Ticker</th>
                        <th scope="col">Asset Class</th>
                        <th scope="col"></th>
                    </tr>
                </thead>
                <tbody data-outlet="list"></tbody>
            </table>
        {{else}}
            <div class="text-center my-4"><em>- empty -</em></div>
        {{/if}}
    </div>
</div>